spring:
  application:
    name: record-server
  cloud:
    config:
      uri: http://192.168.0.222:8880
#      discovery:
#        service-id: config-server
#        enabled: true
      profile: dev
      label: master

